#pragma once

enum NodeType {
  START,
  LOCK,
  UNLOCK,
  READ,
  WRITE,
  FUNCTION_CALL,
  THREAD_CREATE,
  THREAD_JOIN,
  STARTWHILE,
  WHILE,
  ENDWHILE,
  BREAK,
  CONTINUE,
  CONTINUE_RETURN,
  IF,
  ENDIF,
  SWITCH,
  ENDSWITCH,
  RETURN,
  ERASER_IGNORE_ON,
  ERASER_IGNORE_OFF
};